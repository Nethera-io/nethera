# Self-host JupyterLab with a public URL

Example Nethera app for the [jupyter recipe](https://nethera.io/docs/recipes/jupyter).

## Deploy

From this example directory, run:

```bash
neth init
neth deploy
```

Open the endpoint printed by `neth deploy`. Nethera login protects the JupyterLab endpoint.

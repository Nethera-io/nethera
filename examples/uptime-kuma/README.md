# Self-host Uptime Kuma with a public URL

Example Nethera app for the [uptime-kuma recipe](https://nethera.io/docs/recipes/uptime-kuma).

## Deploy

From this example directory, run:

```bash
neth init
neth deploy
```

Open the endpoint printed by `neth deploy`, then create the first Uptime Kuma admin account.
